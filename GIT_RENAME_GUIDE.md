# คู่มือการเปลี่ยนชื่อบน Git

## 📋 สรุปผลกระทบ

### ✅ ไม่กระทบโปรเจกต์ Local

การเปลี่ยนชื่อต่อไปนี้ **ไม่กระทบ** โปรเจกต์ local ของคุณ:

1. **เปลี่ยนชื่อ Git Username** (GitHub/GitLab username)
2. **เปลี่ยนชื่อ Repository** (repo name บน GitHub/GitLab)
3. **เปลี่ยนชื่อ Folder/Project** (เช่น `HRM` → `HRM-New`)

### ⚠️ ต้องอัปเดต Remote URL

ถ้าเปลี่ยน username หรือ repository name ต้องอัปเดต remote URL:

```bash
# ดู remote URL ปัจจุบัน
git remote -v

# อัปเดต remote URL
git remote set-url origin https://NEW_USERNAME@github.com/NEW_USERNAME/NEW_REPO_NAME.git
```

---

## 🔄 วิธีเปลี่ยนชื่อแต่ละแบบ

### 1. เปลี่ยนชื่อ Git Username (GitHub/GitLab)

**ผลกระทบ:**
- ❌ ไม่กระทบโปรเจกต์ local
- ✅ ต้องอัปเดต remote URL

**ขั้นตอน:**

1. เปลี่ยนชื่อบน GitHub/GitLab (Settings → Account)

2. อัปเดต remote URL:
   ```bash
   # ดู remote URL ปัจจุบัน
   git remote -v
   
   # อัปเดต (เปลี่ยน username)
   git remote set-url origin https://NEW_USERNAME@github.com/NEW_USERNAME/HRM.git
   
   # ตรวจสอบ
   git remote -v
   ```

3. ทดสอบ:
   ```bash
   git push
   ```

---

### 2. เปลี่ยนชื่อ Repository

**ผลกระทบ:**
- ❌ ไม่กระทบโปรเจกต์ local
- ✅ ต้องอัปเดต remote URL

**ขั้นตอน:**

1. เปลี่ยนชื่อ repository บน GitHub/GitLab:
   - GitHub: Settings → Repository name → Rename
   - GitLab: Settings → General → Advanced → Rename repository

2. อัปเดต remote URL:
   ```bash
   git remote set-url origin https://USERNAME@github.com/USERNAME/NEW_REPO_NAME.git
   ```

3. ทดสอบ:
   ```bash
   git push
   ```

---

### 3. เปลี่ยนชื่อ Folder/Project

**ผลกระทบ:**
- ❌ ไม่กระทบ Git repository
- ⚠️ ระวัง path ใน IDE/terminal

**ขั้นตอน:**

1. ปิด IDE/Editor ที่เปิดโปรเจกต์อยู่

2. เปลี่ยนชื่อ folder:
   ```bash
   # Windows
   ren HRM HRM-New
   
   # หรือใช้ File Explorer เปลี่ยนชื่อ
   ```

3. เปิด IDE ใหม่และเปิดโปรเจกต์จาก folder ใหม่

4. Git ยังทำงานปกติ (ไม่ต้องทำอะไรเพิ่ม)

---

### 4. เปลี่ยนชื่อใน Commit History (ไม่แนะนำ)

**ผลกระทบ:**
- ⚠️ กระทบ commit history
- ⚠️ อาจทำให้ conflict ถ้ามีคนอื่น pull ไปแล้ว

**ไม่แนะนำ** เพราะ:
- ต้อง force push (`git push --force`)
- อาจทำให้คนอื่นที่ pull ไปแล้วมีปัญหา
- ควรทำเฉพาะโปรเจกต์ส่วนตัวหรือก่อน push ครั้งแรก

---

## ✅ ตรวจสอบหลังเปลี่ยนชื่อ

```bash
# ตรวจสอบ remote URL
git remote -v

# ตรวจสอบว่า push/pull ยังทำงานได้
git fetch
git status
```

---

## 🆘 ปัญหาที่พบบ่อย

### 1. "remote: Permission denied"

**สาเหตุ:** Remote URL ยังใช้ username เก่า

**แก้ไข:**
```bash
git remote set-url origin https://NEW_USERNAME@github.com/NEW_USERNAME/REPO.git
```

### 2. "repository not found"

**สาเหตุ:** Repository name เปลี่ยนแล้วแต่ remote URL ยังเป็นชื่อเก่า

**แก้ไข:**
```bash
git remote set-url origin https://USERNAME@github.com/USERNAME/NEW_REPO_NAME.git
```

### 3. IDE ไม่เจอโปรเจกต์

**สาเหตุ:** เปลี่ยนชื่อ folder แล้ว IDE ยังเปิด path เก่า

**แก้ไข:**
- ปิด IDE
- เปิด IDE ใหม่
- Open Folder → เลือก folder ใหม่

---

## 📝 สรุป

| การเปลี่ยนชื่อ | กระทบ Local? | ต้องอัปเดต? | วิธีแก้ |
|---------------|-------------|------------|---------|
| Git Username | ❌ ไม่ | ✅ ใช่ | `git remote set-url` |
| Repository Name | ❌ ไม่ | ✅ ใช่ | `git remote set-url` |
| Folder Name | ❌ ไม่ | ❌ ไม่ | เปลี่ยนชื่อ folder |
| Commit Author | ⚠️ ใช่ | ⚠️ ใช่ | `git rebase` (ไม่แนะนำ) |

---

## 💡 คำแนะนำ

1. **เปลี่ยนชื่อ username/repo:** ปลอดภัย ไม่กระทบ local
2. **เปลี่ยนชื่อ folder:** ปลอดภัย แต่ต้องเปิด IDE ใหม่
3. **เปลี่ยน commit history:** ไม่แนะนำ (ทำเฉพาะโปรเจกต์ส่วนตัว)

**สรุป:** การเปลี่ยนชื่อบน Git **ไม่กระทบโปรเจกต์ local** แต่ต้องอัปเดต remote URL หลังเปลี่ยน username หรือ repository name

